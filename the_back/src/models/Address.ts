// Specifies which attributes an Address must have.

interface Address {
	id: string;
	address: string;
	number: string;
	complement: string;
	zipcode: string;
	city: string;
	state: string;
}

export default Address;
